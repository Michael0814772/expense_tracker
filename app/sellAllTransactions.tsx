import React from "react";
import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const sellAllTransactions = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>sellAllTransactions</Text>
      </View>
    </SafeAreaView>
  );
};

export default sellAllTransactions;
